conf t

! Remove 192.168.1.0/24 from OSPF on RT1
router ospf 1
 no network 192.168.1.0 0.0.0.255 area 0
 exit

! ACL matching the internal network to be translated
ip access-list extended RT1-NAT
 remark Match internal hosts for NAT (192.168.1.0/24)
 permit ip 192.168.1.0 0.0.0.255 any
exit

! Mark interfaces for NAT - set inside/outside appropriately
interface GigabitEthernet0/0/0
 ip nat outside
 exit
interface GigabitEthernet0/0/1
 ip nat inside
 exit

! Configure PAT (overload) using the outside interface
ip nat inside source list RT1-NAT interface GigabitEthernet0/0/0 overload

end
write memory
