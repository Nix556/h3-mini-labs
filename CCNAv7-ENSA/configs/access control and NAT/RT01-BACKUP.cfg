! RT01 Backup TFTP configuration
! Sets the TFTP source interfaces for RT01 so backups use the correct source IP
conf t
! Use both interfaces so either source IP may be used depending on route/source selection
ip tftp source-interface GigabitEthernet0/0/0
ip tftp source-interface GigabitEthernet0/0/1
end
write memory
